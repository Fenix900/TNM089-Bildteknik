function result = removeNaN(image)
[row, col] = size(image);
nan_exists = true;

%Kolla om det finns NaN
for r = 1:row
    for c = 1:col
        if isnan(image(r,c))
            
        end
     
    end
end

result = image;
end

